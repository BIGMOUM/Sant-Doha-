
{% block body %}


    <div class="block">
        <h4 class="title">Derniers articles</h4>
        <ul class="list">
            {% for categorie in categories %}
                <li class="list-item">
                 <span class="post-title">{{ categories.nom}}</span>
                    <span class="post-date">({{ categories.createdAt | date('d/m/Y') }})</span>
                </li>
            {% endfor %}
        </ul>

    </div>

    <div class="block">
        <h4 class="title"> Categories</h4>
        <ul class="list">
            {% for category in categories %}
                <li class="list-item">{{ category.nom }}</li>
            {% endfor %}
        </ul>
    </div>

{% endblock %}

{% block title %}

{% endblock %}

